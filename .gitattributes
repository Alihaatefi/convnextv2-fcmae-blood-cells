# Normalize line endings so the repo stays LF regardless of contributor OS.
* text=auto eol=lf

# Binary assets: never diff or normalize.
*.png binary
*.pdf binary
*.pt binary

# Notebooks are JSON but diff badly; mark as text without merge attempts.
*.ipynb -merge
